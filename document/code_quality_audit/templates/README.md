# 模板说明

本目录存放“可直接复制使用”的模板，用于保证不同负责人输出的模块报告/全局汇总具备一致结构与口径。

## 文件列表

- `module_report.md`：模块排查报告模板（每个模块一个文件）
- `global_summary.md`：全局汇总模板（跨模块去重合并、分配全局 ID）

## 使用建议

1. 新建模块报告时，优先以 `module_report.md` 为基准生成骨架（也可由初始化脚本自动生成）。
2. 不要在各模块报告中自行发明字段/表格列；如需扩展，先在模板中统一扩展，并同步到所有模块报告。

